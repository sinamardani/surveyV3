import { Routes } from '@angular/router';
import { ReportdesignComponent } from './components/reportdesign/reportdesign.component';

export const routes: Routes = [
  { path: 'ReportDesign', component: ReportdesignComponent },
];
